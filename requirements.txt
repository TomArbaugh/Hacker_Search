# --- runtime (needed on the server) ---
numpy<2
sentence-transformers>=2.7
chromadb>=0.5
rank-bm25>=0.2
flask>=3.0
gunicorn>=21.0

# --- offline pipeline only (ingest.py / build_index.py on your machine) ---
requests>=2.31
python-dotenv>=1.0
tqdm>=4.66
